# ✅ Чеклист готовности к деплою на Vercel

## ✅ Выполнено

- [x] **Схема БД**: `prisma/schema.prisma` переключена на PostgreSQL
- [x] **Убран демо-текст**: Удалено упоминание "markaz / 2020331" из страницы входа
- [x] **Реальный админ**: Seed использует переменные окружения `ADMIN_LOGIN` и `ADMIN_PASSWORD`
- [x] **Build скрипт**: Добавлен `prisma generate && prisma migrate deploy` в `build`
- [x] **Postinstall**: Добавлен `postinstall` для автоматической генерации Prisma Client
- [x] **Prisma seed**: Настроен в `package.json` для автоматического запуска
- [x] **.vercelignore**: Создан для исключения локальных файлов

---

## 🚀 Шаги для деплоя на Vercel

### 1. Создай PostgreSQL базу в Vercel

1. Открой проект в Vercel → вкладка **"Storage"**
2. Нажми **"Create Database"** → выбери **"Postgres"**
3. Создай БД (бесплатный план Hobby)

### 2. Настрой переменные окружения в Vercel

В **Settings → Environment Variables** добавь:

| Переменная | Значение | Откуда взять |
|-----------|----------|--------------|
| `DATABASE_URL` | `postgresql://...` | Из Vercel Postgres (скопируй `POSTGRES_PRISMA_URL`) |
| `NEXTAUTH_URL` | `https://твой-домен.vercel.app` | Твой домен на Vercel |
| `NEXTAUTH_SECRET` | Случайная строка | Сгенерируй: `openssl rand -base64 32` |
| `ADMIN_LOGIN` | `admin` (или свой) | Логин для первого админа |
| `ADMIN_PASSWORD` | Твой пароль | **Безопасный пароль!** |

### 3. Запушь код

```bash
git add .
git commit -m "feat: готово к деплою на Vercel - PostgreSQL, реальный админ"
git push
```

### 4. Vercel автоматически:

- ✅ Установит зависимости (`npm install`)
- ✅ Запустит `postinstall` → `prisma generate`
- ✅ Запустит `build` → `prisma migrate deploy` + `next build`
- ✅ Задеплоит приложение

### 5. После первого деплоя создай админа

**Вариант А: Через Vercel CLI**

```bash
vercel env pull .env.local
npx prisma db seed
```

**Вариант Б: Через Vercel Dashboard**

1. Открой проект → вкладка **"Deployments"**
2. Найди последний деплой → **"View Function Logs"**
3. Или используй **"Shell"** (если доступно) для выполнения команд

**Вариант В: Вручную через SQL**

Подключись к PostgreSQL через Vercel Dashboard → Storage → Postgres → "Connect" и выполни:

```sql
-- Создай админа вручную (пароль должен быть захеширован через bcrypt)
-- Или используй seed скрипт через CLI
```

---

## 🔍 Проверка после деплоя

1. ✅ Открой `https://твой-домен.vercel.app/login`
2. ✅ Войди с логином и паролем из `ADMIN_LOGIN` / `ADMIN_PASSWORD`
3. ✅ Проверь, что админ-панель открывается
4. ✅ Проверь, что можно создавать регионы и кандидатов

---

## ⚠️ Если что-то не работает

### Ошибка "Неверный логин или пароль"

- Проверь, что `DATABASE_URL` правильный в Vercel
- Проверь, что миграции применены (в логах деплоя должно быть `prisma migrate deploy`)
- Проверь, что seed выполнен (пользователь создан в БД)

### Ошибка подключения к БД

- Проверь `DATABASE_URL` в Environment Variables
- Убедись, что PostgreSQL создан в Vercel Storage
- Проверь логи деплоя на ошибки Prisma

### Build падает

- Проверь логи в Vercel → Deployments → последний деплой
- Убедись, что `prisma` в `dependencies` (не в `devDependencies`)
- Проверь, что `schema.prisma` использует `provider = "postgresql"`

---

## 📝 Важные заметки

- **Пароль админа**: Установи `ADMIN_PASSWORD` в Vercel Environment Variables **до первого деплоя**, чтобы seed создал правильного пользователя
- **NEXTAUTH_SECRET**: Должен быть одинаковым для всех деплоев (production, preview)
- **DATABASE_URL**: Используй `POSTGRES_PRISMA_URL` из Vercel Postgres для лучшей совместимости

---

## 🎉 Готово!

После выполнения всех шагов приложение будет работать на Vercel с PostgreSQL и реальным админом.
